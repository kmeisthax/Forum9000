{% macro permalink_forum(forum, page) %}
    {{url('f9kforum_forum', {'slug': forum.slug, 'page':page|default(1)})}}
{% endmacro %}

{% macro permalink_forum_action(forum, action) %}
    {{url('f9kforum_forum_' ~ action, {'slug': forum.slug})}}
{% endmacro %}
